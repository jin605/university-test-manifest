apiVersion: v1
kind: Service
metadata:
  name: university-vue-service
  namespace: university
spec:
  # type: NodePort
  selector:
    app: university-vue
  ports:
  - port: 80
    targetPort: 80
    # nodePort: 30080
