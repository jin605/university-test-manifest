apiVersion: v1
kind: Service
metadata:
  name: department-api-service
  namespace: university
spec:
  # type: NodePort
  selector:
    app: department-api
  ports:
  - port: 8088
    targetPort: 8088
    # nodePort: 30021
